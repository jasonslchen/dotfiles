#!/bin/sh


# ZSH {
# Change to zsh
is_interactive && {
	if [[ "$SHELL" != "$(which zsh)" ]]; then
		sudo chsh -s $(which zsh)
	fi
}


function link_files() {
  # Git link
  rm -f ~/.gitconfig
  ln -s $(pwd)/.gitconfig ~/.gitconfig
}

